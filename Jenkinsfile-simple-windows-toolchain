pipeline {
  agent any
  stages {
    stage("Build") {
      steps {
        bat "echo build"
      }
    }
    stage("SonarQube Scan") {
      steps {
        bat "echo sonarqube"
      }
    }
    stage("Push to Artifactory") {
      steps {
        bat "echo artifactory"
      }
    }
  }
}
